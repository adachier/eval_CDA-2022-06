<form method="post" action="{{ path('app_computer_delete', {'id': computer.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ computer.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
